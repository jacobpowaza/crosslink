{
  "name": "crosslink-docs",
  "private": true,
  "scripts": {
    "check": "mint validate && mint broken-links --check-anchors --check-redirects"
  },
  "devDependencies": {
    "@mintlify/cli": "4.0.1444"
  }
}
